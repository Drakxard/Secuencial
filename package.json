{
  "name": "secuencial",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "web:prepare": "node scripts/prepare-web.mjs",
    "android:sync": "npm run web:prepare && cap sync android",
    "android:build": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "test": "npm run web:prepare && playwright test",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/filesystem": "8.1.3",
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "@playwright/test": "1.62.1"
  }
}
